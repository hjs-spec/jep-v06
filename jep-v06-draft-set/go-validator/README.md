# Go Validator Seed

Minimal Go structural validator seed. It computes deterministic seed canonicalization and event hash,
and checks core structural fields and detached JWS compact shape.

It does not perform Ed25519 verification yet.
