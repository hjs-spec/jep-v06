[project]
name = "jep-v06-conformance-seed"
version = "0.6.0"
description = "JEP v0.6 draft set conformance seed"
requires-python = ">=3.10"
dependencies = ["cryptography>=42"]

[project.optional-dependencies]
test = ["pytest>=8"]
